import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'liveMenu-customer',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
